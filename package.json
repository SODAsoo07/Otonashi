{
  "name": "otonashi",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "desktop:dev": "electron .",
    "engine:build": "tsc -p tsconfig.engine.json",
    "otonashi:render": "npm run engine:build --silent && node dist-engine/scripts/render-vowel-only.js",
    "otonashi:service": "npm run engine:build --silent && node dist-engine/scripts/vowel-engine-service.js",
    "test:vowel-renderer": "npm run engine:build --silent && node dist-engine/scripts/test-vowel-renderer.js",
    "test:vowel-service": "npm run engine:build --silent && node dist-engine/scripts/test-vowel-service.js",
    "build:desktop:win": "npm run build && electron-builder --win --publish never",
    "build:desktop:mac": "npm run build && electron-builder --mac --publish never"
  },
  "build": {
    "appId": "com.sodasoo07.otonashi",
    "productName": "OTONASHI",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asar": true,
    "npmRebuild": false,
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "identity": null
    },
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    }
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "lucide-react": "^0.564.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
